/* extern_call_without_marker.jazz: extern called without @ 
 * EXPECTED: fail 
 * MESSAGE: typing error: unknown function: `add'             */
extern fn add(u64 x, u64 y) -> u64;

export fn main(reg u64 a, reg u64 b) -> reg u64 {
  reg u64 r;
  r = add(a, b);
  return r;
}
