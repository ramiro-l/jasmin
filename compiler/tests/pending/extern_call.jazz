/* extern_call.jazz: call to an extern fn with @ 
 * EXPECTED: success                                                          */
extern fn add(u64 x, u64 y) -> u64;

export fn main(reg u64 a, reg u64 b) -> reg u64 {
  reg u64 r;
  r = @add(a, b);
  return r;
}
