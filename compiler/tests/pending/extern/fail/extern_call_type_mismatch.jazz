/* extern_call_type_mismatch.jazz: calling external function with mismatched argument type
 * EXPECTED: fail 
 * MESSAGE: typing error: can not implicitly cast bool into u64 */
extern fn add(u64 x, u64 y) -> u64;

export fn main(reg u64 a) -> reg u64 {
  reg u64 r;
  reg bool b;
  b = true;
  r = @add(a, b);
  return r;
}
